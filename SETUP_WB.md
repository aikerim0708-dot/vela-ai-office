# Подключение WB-кабинета к AI Office

## Что ты получишь после этой настройки

- **Лео** видит реальные данные кампаний 29230612 и 32284868. Сам тянет с WB по запросу «дай ДРР за вчера».
- **Анна** видит остатки и продажи по топ-30 SKU. Считает дни покрытия.
- **Зара** видит выручку, расход рекламы, заказы.
- **Ева** читает свежие отзывы без ответа.
- Любой агент может прочитать **загруженный тобой Excel** (Wildbox, MPSTATS, выгрузки которых нет в API).

Время настройки: **5–10 минут**.

---

## Шаг 1. Получить API-токен WB (5 минут)

1. Зайди в **WB Seller** → правый верх → клик по аватарке → **Настройки**
2. Слева в меню → **Доступ к API**
3. Кнопка **«Создать новый токен»**
4. Введи имя: `AI Office` (или любое)
5. **Тип токена: Только чтение** (Read Only) — это безопаснее, ничего изменить не сможет
6. **Поставь галочки на категории:**
   - ☑ Контент
   - ☑ Аналитика
   - ☑ Статистика
   - ☑ Продвижение
   - ☑ Отзывы и вопросы
7. Жми **«Создать токен»**
8. **Скопируй токен СРАЗУ** — WB покажет его один раз. Это длинная JWT-строка вида `eyJhbGciOiJFUzI1NiIs...`

> ⚠️ Этот токен — как ключ от квартиры. Никому не давай. Лежит у тебя только в файле `backend/.env` на твоём Маке.

---

## Шаг 2. Положить токен в .env

1. Открой Finder → перейди в папку `~/Documents/Claude/Projects/ИИ офис/vela-ai-office/backend/`
2. Найди файл `.env` (если скрыт — Cmd+Shift+. покажет скрытые)
3. Открой его в **TextEdit** (правый клик → Открыть с помощью → TextEdit)
4. Добавь строку в самый конец:
   ```
   WB_API_TOKEN=<вставь сюда свой токен>
   ```
5. Сохрани (Cmd+S)

Должно выглядеть так:
```
VELA_PROVIDER=claude
ANTHROPIC_API_KEY=sk-ant-api03-XXXXXXXXX
VELA_MODEL=claude-sonnet-4-5
WB_API_TOKEN=eyJhbGciOiJFUzI1NiIsImtpZCI6...
```

---

## Шаг 3. Установить новые библиотеки и перезапустить

Открой **новое** окно Терминала (не то где работает uvicorn). Выполни по очереди:

```bash
cd ~/Documents/Claude/Projects/"ИИ офис"/vela-ai-office/backend
source .venv/bin/activate
pip install -r requirements.txt
```

(Должно поставить `httpx`, `openpyxl`, `pandas` — это минута)

Теперь **останови старый uvicorn** (в первом окне Терминала — Ctrl+C) и запусти заново:

```bash
uvicorn app:app --reload --port 8000
```

---

## Шаг 4. Проверить что WB-токен работает

1. Открой в браузере `http://localhost:8000`
2. Под полем «цель Максу» появился блок **🛒 WB-кабинет**
3. Жми **🔄 Проверить токен**
4. Если видишь **🟢 токен живой** — всё ок, переходи к шагу 5
5. Если **🔴 401** — токен неправильный, перевыпусти
6. Если **🔴 403** — у токена нет нужных категорий, в WB добавь галки

Альтернатива: открой `http://localhost:8000/api/wb/health` напрямую — увидишь JSON-ответ.

---

## Шаг 5. Тест на Лео

1. На главной кликни на **🦁 Лео** в карточках команды
2. В поле задачи вставь:
   ```
   Дай ДРР за вчера по обеим кампаниям (29230612 и 32284868),
   найди самые дорогие кластеры и предложи что подкрутить.
   ```
3. Жми отправить. **Ждёшь 30–90 секунд.**
4. Лео сам дёрнет WB → получит реальные числа → разберёт

Что должен увидеть в ответе:
- Цифры ДРР, расхода, выручки **точно совпадают** с тем что в кабинете WB
- Топ-кластеры с CTR/CR/расходом
- Конкретные предложения «было → станет, причина, ожидаемый эффект»

Если Лео пишет «нет данных» или «не могу дотянуться до WB» — что-то с токеном, иди в шаг 4.

---

## Шаг 6. Загрузка Excel

На главной → блок «WB-кабинет» → **📎 Прикрепить Excel/CSV**.

Грузишь любой `.xlsx`, `.xls`, `.csv` (например выгрузку из Wildbox или MPSTATS).

В задаче Максу или Лео пиши:
```
Прочитай файл «отчёт_Wildbox_май.xlsx» и сравни ДРР по кластерам с тем что
в кампании 29230612 за тот же период.
```

Агент сам вызовет инструмент `read_uploaded_file` + `wb_campaign_stats` и сравнит.

---

## Какие endpoint WB сейчас покрыты

| Инструмент | Какой агент | Что делает |
|---|---|---|
| `wb_list_campaigns` | Лео | Список всех кампаний с их статусом |
| `wb_campaign_info` | Лео | Детали кампаний (бюджет, СПП, привязанные SKU) |
| `wb_campaign_stats` | Лео, Зара | Статистика за период: ДРР, CTR, CR, разбивка по кластерам |
| `wb_stocks` | Анна | Остатки по SKU и складам |
| `wb_sales_recent` | Анна, Зара | Заказы за последние N дней |
| `wb_unanswered_feedbacks` | Ева | Новые отзывы без ответа |
| `read_uploaded_file` | все | Чтение Excel/CSV из uploads |
| `list_uploaded_files` | все | Какие файлы загружены |

---

## Что НЕ покрыто (Этап 2)

- Изменение ставок (только черновики через approval — мы НЕ даём агентам писать в WB)
- Полный финансовый отчёт (детали по комиссиям/штрафам) — endpoint `/api/v5/supplier/reportDetailByPeriod` нужно добавить
- Контент карточек (фото, описания) — добавим когда Ева начнёт активно работать
- Аналитика воронки карточек (показы → переходы → корзина → заказ) — нужен content-api

Если что-то из этого срочно — скажи, докручу за 1–2 часа на endpoint.
